@model  UserAccessGroupsModel

@{
    Layout = "_CrudSingleColumn";
    ViewData["WindowTitle"] = "User Access Groups - Edit";
    ViewData["PageTitle"] = $"User {Model?.UserName} - Edit Access Groups";
}

<form asp-action="EditAccessGroups">
    <table class="table table-condensed table-bordered">
        <thead>
            <tr>
                <th>Name</th>
                <th>Value</th>
            </tr>
        </thead>
        <tbody>
            @if (Model?.AccessGroups.Count() == 0)
            {
                <tr><td colspan="2" class="text-center">There are currently no Access Groups.</td></tr>
            }
            else
            {
                for (int i = 0; i < Model?.AccessGroups.Count(); i++)
                {
                    <tr>
                        <td>
                            <input type="hidden" asp-for="@Model.AccessGroups[i].Id" />
                            <input asp-for="@Model.AccessGroups[i].Name" class="form-control" />
                        </td>
                        <td>
                            <input asp-for="@Model.AccessGroups[i].RelationshipPresent" type="checkbox" class="form-control" />
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>

    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
</form>

<div>
    <a asp-controller="Users" asp-action="Index">Back to Users</a>
</div>

